const glassEffectSupported = "@supports (backdrop-filter: blur())";
const glassEffectNotSupported = "@supports (not (backdrop-filter: blur()))";

export { glassEffectSupported, glassEffectNotSupported };
